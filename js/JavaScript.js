const surfaceAreaCalculator = (number) => {
   return 4 * 3.14 * number * number;
}

const surfaceOfMars = surfaceAreaCalculator(1043430);

const dimatest = surfaceAreaCalculator(82);
console.log(surfaceOfMars)
console.log(dimatest)
// --------Я делал попытки---------------------
const yourAge = (number) => {
   return 100 - 50 + number;
};
const dimaAge = yourAge(22);

console.log(dimaAge)
console.log(dimaAge)


const percentaCalculator = (number, total) => {
   return number * 100 / total;
}
const winterDone = percentaCalculator(2, 3);
console.log(percentaCalculator(5, 28));

// ----------------------------